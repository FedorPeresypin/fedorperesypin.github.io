import 'package:flutter/material.dart';

/// Страница процесса
class ProcessPage extends StatelessWidget {
  const ProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Process Page'));
  }
}
